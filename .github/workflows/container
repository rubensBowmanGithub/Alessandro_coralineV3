name: CI
 
on: push
 
jobs:
  linux:
    strategy:
      matrix:
        container:
          - 'ubuntu:latest'
          - 'centos:latest'
    runs-on: ubuntu-latest
    container: ${{ matrix.container }}
    steps:
      - run: cat /etc/*release
