name: CI

on: push

jobs:
  linux:
    strategy:
      matrix:
        container:
          - 'centos:7'
          - 'centos:latest'
    runs-on: ubuntu-latest
    container: ${{ matrix.container }}
    steps:
      - run: cat /etc/*release
     
